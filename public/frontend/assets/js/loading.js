'use strict';

/*
---------------------------------------
Loader
---------------------------------------
*/

$(window).on('load', function () {
    let load_screen = document.getElementById("loading");
    $(load_screen).remove();
});
