'use strict';

/*
---------------------------------------
Loader
---------------------------------------
*/

$(window).on('load', function () {
    let loadDingScreen = document.getElementById("loading");
    $(loadDingScreen).remove();
});
